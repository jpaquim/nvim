require("lazy.core.util").warn(
  "`flash.nvim` is now the default jump plugin for **LazyVim**.\nPlease remove the `plugins.extras.editor.flash` import from your **lazy** config.",
  { title = "LazyVim" }
)

return {}
